#include <stdio.h>
#include <stdlib.h>
#include "filme.h"

Lista *CriarListaFreq(){
    return NULL;
}

int inserirListaFreq(No *raiz_filme, NoLista **p, elem *x){
    return 0;
}

int inserir_filme(No **p, elem *x){
    return inserir(p, x, Filme);
}

int remover_filme(No **p, elem *x){
    return remover(p, x);
}

void mais_queridos(){
    return;
}
